rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 사용자 본인의 파일만 읽기·쓰기 허용
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == uid;
    }
  }
}
